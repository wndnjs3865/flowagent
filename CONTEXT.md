# CONTEXT — flowagent 도메인 용어집

코드와 화면에서 같은 의미로 같은 단어를 쓰기 위한 사전. 새로운 도메인 개념을 코드/문서에 도입하기 전에 여기서 먼저 합의한다.

ADR-0001에서 채택한 ubiquitous-language glossary. 무겁게 시작하지 말고, 충돌이 생기는 단어부터 천천히 채워간다.

---

## 결과 (Result)

워크플로를 한 번 돌렸을 때 만들어진 한 덩어리. 사용자(사장님)가 머릿속에서 자연스럽게 부르는 단어.

**무엇이 포함되나** (현재 정의 — 2026-05-20):

- 어느 워크플로의 것인지 (`workflowName`)
- 언제 만들어졌는지 (`startedAt`, ISO-8601 UTC)
- 무엇이 나왔는지 (`lastOutput` — 마지막 step의 출력)
- (식별자) `runId`

**무엇이 포함되지 않나** (의도적):

- step별 진행 상황 — 그건 실시간 스트리밍(SSE)으로만 보여주고, 결과에는 최종본만 남음
- 성공/실패 플래그 — Result 타입 안에는 없다. 대신 **reader(`src/results.ts`)가 완료되고 성공한 실행만 Result로 반환**한다. 실패한 실행과 in-flight 실행은 대시보드/공유에 노출되지 않는다. 향후 "최근 실패도 보고 싶다" 같은 요구가 생기면 별도 query (`getLatestIncludingFailed` 등)로 추가.

**deferred** (YAGNI):

- `ResultDetail` (step별 출력 포함된 무거운 projection) — grilling 단계에서 "둘 다 제공" 합의했지만 현재 caller가 없음. 첫 caller(결과 상세 페이지 등) 등장 시 추가.

**어디에 보이나**:

- 사장 대시보드(`/executive`) — 카드 1개 = 결과 1개의 미리보기
- 공유 페이지(`/share/?t=...`) — 외부에 보낸 결과의 전체 보기
- (실행 페이지 `/workflows/<slug>`는 결과를 **만드는 중**의 화면이지, 저장된 결과를 보여주는 화면이 아니다)

**용어 충돌 주의**:

- `runner.ts`의 `RunResult = { runId, ok }`는 도메인 "결과"와 다르다. 후자는 "워크플로가 만들어낸 산출물(=결과)", 전자는 "실행 절차의 성공/실패 여부". 코드 리팩터 시 `RunResult` → `RunOutcome` 같은 이름으로 분리 검토.
- 영어 매핑은 `Result`. 모듈 이름은 컨텍스트 따라 `WorkflowResult`, `RunResult`(도메인) 등이 후보.

---

## 워크플로 (Workflow)

`workflows/<slug>.yaml`로 정의된 단계 시퀀스. 한 번 정의되면 여러 번 실행될 수 있다. **워크플로 1개에 결과 N개**의 관계.

---

## 실행 (Run)

워크플로를 한 번 돌리는 행위. 동사적. **"실행"의 산출물이 "결과"**.

코드에서 `runner.ts`/`runs/*.jsonl` 등의 "run" 용어는 이 의미. UI에서 "지금 실행 →" 같은 액션 라벨로 쓴다. 저장된 산출물을 가리킬 때는 "실행"이 아니라 "결과"를 쓴다.
