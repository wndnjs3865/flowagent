---
description: Write a short PRD for the change before any code.
---

Load the `spec-driven-development` skill from `.claude/skills/spec-driven-development/SKILL.md`.

If alignment is still loose, run `grill-me` first. Then draft the spec using the template in the skill —
Objective, User/caller, In scope, Out of scope, Structure, Verification, Open questions. Keep each section
tight.

Present the spec and wait for explicit approval before proceeding to code.
